name: Go cicd
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:   
    runs-on: self-hosted
    steps:  
    - name: build_push_tag of feature/sj
      env:      
       PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
       USERNAME: ${{ secrets.DOCKER_USERNAME }}  
      run: |
            
             docker login -u $USERNAME -p $PASSWORD
             docker build -t sugamdocker35/secretproj:1.0 -f Dockerfile .  
             docker push sugamdocker35/secretproj:1.0 
  deploy:
    runs-on: self-hosted
    steps:
    - name: deploy app
      if: contains('refs/heads/main',github.ref)
      env:
       PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
       USERNAME: ${{ secrets.DOCKER_USERNAME }}  
      run: |
              docker login -u $USERNAME -p $PASSWORD  
              docker run sugamdocker35/secretproj:1.0
